# Template: index-shell.md

Landing page / app map template for `index.html` (prototype root). **CDN-free CSS.**
`index.html` is static — no `x-data` on `<main>`. Paths are NON-prefixed (root level).
Use ONLY the class vocabulary from `design-system-ref.md` — no Tailwind utility classes.

---

```html
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>APP_TITLE — Prototype</title>
  <link rel="stylesheet" href="css/tokens.css">
  <link rel="stylesheet" href="css/base.css">
  <link rel="stylesheet" href="css/components.css">
  <script src="js/app.js" defer></script>
  <script src="js/navigation.js" defer></script>
  <script src="https://cdn.jsdelivr.net/npm/alpinejs@3.x.x/dist/cdn.min.js" defer></script>
</head>
<body x-data x-cloak>
  <div class="app">

    <!-- ═══════ Sidebar Navigation ═══════ -->
    <aside class="sidebar">
      <div class="sidebar-header">
        <span class="sidebar-brand">APP_TITLE</span>
        <span class="badge badge-primary">prototype</span>
      </div>
      <nav class="sidebar-nav" aria-label="Main navigation">
        <!--
          NAV_ITEMS_BLOCK — generated by assembly-wiring from nav_structure.
          Pattern per domain group:
            <div class="nav-group">
              <p class="nav-group-label">DOMAIN</p>
              <a href="pages/PAGE_ID.html" class="nav-item" data-nav-id="PAGE_ID">ICON PAGE_TITLE</a>
            </div>
        -->
      </nav>
    </aside>

    <!-- ═══════ Main Content — App Map ═══════ -->
    <main class="main">
      <div class="content">

        <div class="page-header">
          <div>
            <h1 class="page-title">APP_TITLE</h1>
            <p class="page-subtitle">Select a screen to explore the prototype.</p>
          </div>
          <div class="page-actions">
            <span class="badge badge-default">PAGE_COUNT screens</span>
          </div>
        </div>

        <!-- Page cards grid -->
        <div class="card-grid">
          <!--
            PAGE_CARDS_BLOCK — one card per page, generated by assembly-wiring.
            Pattern per page:
              <a href="pages/PAGE_ID.html" class="card card-link">
                <div class="card-header">
                  <span class="font-semibold">ICON PAGE_TITLE</span>
                  <span class="badge badge-default">DOMAIN</span>
                </div>
                <p class="card-content text-sm text-muted">PAGE_DESCRIPTION</p>
              </a>
          -->
        </div>

      </div>
    </main>
  </div>

  <!-- Toast notifications -->
  <div class="toast-container" aria-live="polite">
    <template x-for="n in $store.notification.items" :key="n.id">
      <div class="alert" :class="n.type === 'error' ? 'alert-destructive' : (n.type === 'success' ? 'alert-success' : '')"
           x-text="n.message" role="status"></div>
    </template>
  </div>

</body>
</html>
```

---

## Note on CSS/JS paths

`index.html` lives at the prototype root, so paths do NOT use `../`:
- `css/tokens.css` ✓  (not `../css/tokens.css`)
- `js/app.js` ✓        (not `../js/app.js`)
- Page links: `pages/{id}.html` ✓

This is the opposite of `page-shell.md`, which uses `../` prefixes.

## Placeholder reference

| Placeholder | Replace with | Example |
|-------------|-------------|---------|
| `APP_TITLE` | Feature/app title | `Scheduled Orders` |
| `PAGE_COUNT` | Total number of pages | `6` |
| `NAV_ITEMS_BLOCK` | Generated from nav_structure | See comment |
| `PAGE_CARDS_BLOCK` | Generated from pages[] | See comment |
