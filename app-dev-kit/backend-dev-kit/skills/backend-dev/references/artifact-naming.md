# Artifact naming — backend-dev

- Slug: `SLUG_HINT`, then kebab(`ENTITY_REFS[0]`), then `TASK_ID`. Never app `metadata.slug`.
- Blackboard: `.spec/backend/<slug>.md`
- Branch: `backend/<slug>`
- Envelope: `.spec/backend/<slug>.kit-result.json`
