---
work-plan-version: "1.0"
spec-ref: "{{SPEC_REF}}"
prototype-ref: "{{PROTOTYPE_REF}}"
generated: "{{TIMESTAMP}}"
updated: "{{TIMESTAMP}}"

tracks: []
tasks: []
---

# Work Plan — {{TITLE}}

Generated from `{{SPEC_REF}}`. Do not hand-edit YAML `tracks[]` / `tasks[]` mid-run —
edit between runs, then re-open with `/orchestrate-app`.

## Log

- {{TIMESTAMP}} — work-plan scaffolded, awaiting analysis
