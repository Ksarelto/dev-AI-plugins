---
checklist-version: "1.0"
spec-ref: "{{SPEC_REF}}"
prototype-ref: "{{PROTOTYPE_REF}}"
generated: "{{TIMESTAMP}}"
updated: "{{TIMESTAMP}}"

tasks: []
---

# Task Checklist — {{TITLE}}

Generated from `{{SPEC_REF}}`. Do not hand-edit the YAML front matter's `tasks[]` while the
orchestrator is mid-run — edit between runs only, then re-open with `/orchestrate-frontend`.

## Log

- {{TIMESTAMP}} — checklist scaffolded, awaiting task derivation
