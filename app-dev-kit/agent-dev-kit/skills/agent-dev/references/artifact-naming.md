# Artifact naming — agent-dev

Slug from `SLUG_HINT` or kebab(agent name). Never app `metadata.slug`.
Blackboard `.spec/agents/<slug>.md`. Branch `agent/<slug>`.
Envelope `.spec/agents/<slug>.kit-result.json`.
