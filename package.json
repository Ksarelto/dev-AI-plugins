{
  "name": "dev-cursor-plugins",
  "version": "0.1.0",
  "private": true,
  "description": "Cursor marketplace with reusable skills, rules, commands, and agents",
  "type": "module",
  "scripts": {
    "validate": "node scripts/validate-marketplace.mjs"
  },
  "devDependencies": {
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1"
  }
}
