{
  "plugin": "agent-dev-kit",
  "source": "./app-dev-kit/agent-dev-kit",
  "component": "agents",
  "cases": [
    {
      "prompt": "Starting a new OpenRouter project I need an architecture: RAG versus chain, vector store choice, and multi-agent topology — do not write implementation yet",
      "expect": "agent-architect"
    },
    {
      "prompt": "Help me choose @openai/agents versus LangGraph.js and a vector store before any code is written",
      "expect": "agent-architect"
    },
    {
      "prompt": "Implement the TypeScript Runner loop, StateGraph if needed, Zod tools, handoffs and guardrails now that architecture is decided",
      "expect": "agent-builder"
    },
    {
      "prompt": "Write the Node tool-calling agent code — Runner or StateGraph — the runtime was already chosen",
      "expect": "agent-builder"
    },
    {
      "prompt": "Implement the TypeScript RAG ingestion path, Qdrant hybrid retrieval, reranking, and citation generation with Vitest golden-set judges",
      "expect": "rag-builder"
    },
    {
      "prompt": "Write the ingestion module, OpenRouter embeddings, Qdrant hybrid retrieve, rerank, and citation generation with Vitest golden-set judges — not an Agent Runner",
      "expect": "rag-builder"
    },
    {
      "prompt": "Coordinate the agent-dev-kit hub-and-spoke pipeline from architecture through scaffold tools Runner eval and RETURN a REVIEW_PACKET without writing src/",
      "expect": "agent-dev-orchestrator"
    },
    {
      "prompt": "Drive the spec-scoped named agent increment on the agent blackboard only — never AskUserQuestion, never write src/",
      "expect": "agent-dev-orchestrator"
    },
    {
      "prompt": "Parse the spec-dev-kit YAML filtered to one agent-surface agent and extract that agent's tools knowledge bases and prototype chat-page path — never write source code",
      "expect": "agent-interpreter"
    },
    {
      "prompt": "Fast read-only parser for one agent-surface row at agent-dev intake before agent-analyst — do not pass the full spec body onward",
      "expect": "agent-interpreter"
    },
    {
      "prompt": "Review the scoped agent blackboard for a testable contract — runtime, tools, embed, eval bar — before architecture",
      "expect": "agent-analyst"
    },
    {
      "prompt": "Return a CLARIFY_PACKET if the agent contract is ambiguous at Station 0 after import-upstream — never write src/",
      "expect": "agent-analyst"
    },
    {
      "prompt": "Run the agent-dev-kit mechanical quality gates typecheck lint vitest optional eval via run-gates.sh and write the Gate Log on the agent blackboard",
      "expect": "quality-gate-runner"
    },
    {
      "prompt": "Execute run-gates.sh for the agent increment and append JSON to Gate Log — never treat a skipped eval script as a pass when the script exists",
      "expect": "quality-gate-runner"
    }
  ]
}
