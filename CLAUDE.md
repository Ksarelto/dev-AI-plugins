# CLAUDE.md

This repo keeps its agent instructions in [AGENTS.md](AGENTS.md), per the
[agents.md](https://agents.md) convention, so Claude Code and every other agent read the same file.

**Read [AGENTS.md](AGENTS.md) before doing anything here.** It covers what the repo is, the commands
(`npm run validate`, `npm run eval` — both must pass), the layout, the invariants, and the known
footguns.

For component formats — rule vs. skill vs. command vs. agent, frontmatter fields, creation flow,
naming, submission checklists — see [CONTRIBUTING.md](CONTRIBUTING.md).
